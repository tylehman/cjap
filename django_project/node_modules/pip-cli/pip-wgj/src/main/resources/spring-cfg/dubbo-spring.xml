<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd 
    http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="consumer-pip-web"  />
 
    <!-- 使用multicast广播注册中心暴露发现服务地址, 从web.properties中读取 -->
    <dubbo:registry id="webregzk" protocol="zookeeper" address=""/>
    
 
    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
    
    <dubbo:reference id="dataServer" interface="com.pip.datacloud.api.DataServer" registry="webregzk"/>
    
    <dubbo:reference id="accountServer" interface="com.pip.accountcloud.api.AccountServer" registry="webregzk"/>
    
    <dubbo:reference id="collisionServer" interface="com.pip.collisionservice.api.CollisionServer" registry="webregzk"/>
    
    <dubbo:reference id="uploadServer" interface="com.pip.uploadcenter.api.UploadServer" registry="webregzk"/>
    
    <dubbo:reference id="dataCollectServer" interface="com.pip.datacollect.api.DataCollectServer" registry="webregzk"/>
    
    <dubbo:reference id="ICallServer" interface="com.pip.icall.ICallServer"  registry="webregzk"/>
    
    <dubbo:reference id="wikiService" interface="com.pip.wiki.api.WikiService"   registry="webregzk"/>
    
</beans>

